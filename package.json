{
  "name": "@quenk/facets-dsl",
  "version": "0.2.2",
  "description": "Small DSL for building search filters",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "clean": "rm -R lib/*",
    "dist": "cd lib && npm publish",
    "build:parser": "./node_modules/.bin/jison -o src/Parser.js src/facets.y",
    "build:src": "./node_modules/.bin/tsc -p src",
    "build": "npm run -s build:parser && npm run -s build:src",
    "test": "./node_modules/.bin/mocha --opts mocha.opts test/parse.ts",
    "docs": "rm -R docs; cd src && ../node_modules/.bin/typedoc --ignoreCompilerErrors --mode modules --out docs . && mv docs .. && cd - && echo 'DO NOT DELETE!' > docs/.nojekyll"
  },
  "author": "Lasana Murray <dev@quenk.com> (http://quenk.com)",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/mocha": "^2.2.41",
    "@types/node": "^7.0.18",
    "jison": "^0.4.17",
    "mocha": "^3.3.0",
    "must": "^0.13.4",
    "ts-node": "^3.0.2",
    "typedoc": "^0.9.0",
    "typescript": "^2.4.2"
  },
  "dependencies": {
    "@quenk/match": "^1.1.0",
    "afpl": "^0.12.0"
  }
}
